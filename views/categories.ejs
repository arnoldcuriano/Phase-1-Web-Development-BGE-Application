<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
   <link rel="stylesheet" href="https://cdn.datatables.net/1.10.21/css/jquery.dataTables.min.css">
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
   <link
      href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet">
   <title>BGE Internal App</title>
   <link rel="icon" type="image/x-icon" href="/images/bge-logo-favicon.png">
   <style>
      body {
         font-family: "Poppins", sans-serif;
         background-color: #f4f4f9;
         margin: 0;
         padding: 0;
         display: flex;
         height: 100vh;
      }

      .sidebar {
         width: 250px;
         background-color: #292929;
         color: white;
         height: 100vh;
         padding: 20px;
         box-sizing: border-box;
         position: fixed;
         top: 0;
         left: 0;
         display: flex;
         flex-direction: column;
         justify-content: flex-start;
      }

      .sidebar .logo {
         margin-bottom: 30px;
         text-align: center;
      }

      .sidebar .logo img {
         width: 80%;
      }

      .sidebar .profile {
         display: flex;
         align-items: center;
         margin-bottom: 30px;
         background-color: #1e1e1e;
         padding: 15px;
         border-radius: 8px;
         overflow: hidden;
      }

      .sidebar .profile img {
         border-radius: 50%;
         width: 60px;
         height: 60px;
         margin-right: 10px;
      }

      .sidebar .profile div {
         display: flex;
         flex-direction: column;
         align-items: flex-start;
         width: 100%;
      }

      .sidebar .profile div h3 {
         margin: 0;
         font-size: 16px;
      }

      .sidebar .profile div p {
         margin: 2px 0;
         font-size: 12px;
         color: #bbb;
         word-wrap: break-word;
      }

      .sidebar .profile div .role {
         background-color: #4caf50;
         color: white;
         padding: 2px 5px;
         border-radius: 5px;
         font-size: 12px;
      }

      .sidebar nav {
         display: flex;
         flex-direction: column;
      }

      .sidebar nav a {
         color: white;
         text-decoration: none;
         display: flex;
         align-items: center;
         padding: 10px 15px;
         margin: 5px 0;
         border-radius: 4px;
         transition: background 0.3s;
      }

      .sidebar nav a:hover {
         background-color: #34495e;
      }

      .sidebar nav a i {
         margin-right: 10px;
      }

      .sidebar .submenu {
         padding-left: 20px;
      }

      .content {
         margin-left: 270px;
         padding: 20px;
         box-sizing: border-box;
         flex: 1;
         overflow-y: auto;
      }

      .header {
         display: flex;
         justify-content: space-between;
         align-items: center;
         margin-bottom: 20px;
      }

      .header h1 {
         margin: 0;
         font-size: 24px;
      }

      .announcement-container {
         background-color: #e3f2fd;
         padding: 20px;
         border-radius: 8px;
         display: flex;
         align-items: center;
         justify-content: space-between;
         margin-bottom: 20px;
         border: 1px solid #42a5f5;
      }

      .announcement-container p {
         margin: 0;
         flex: 1;
      }

      .overview,
      .metrics {
         display: flex;
         flex-wrap: wrap;
         gap: 20px;
         margin-bottom: 20px;
      }

      .card {
         background-color: white;
         padding: 20px;
         border-radius: 8px;
         box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
         flex: 1 1 calc(33.333% - 20px);
         box-sizing: border-box;
         justify-content: center;
         align-items: center;
         text-align: center;
      }

      .card.full-width {
         flex: 1 1 100%;
      }

      .card h3,
      .card h1 {
         margin: 0 0 10px 0;
      }

      .card p {
         margin: 5px 0;
      }

      .card button {
         background: linear-gradient(45deg, #34A853, #4285F4);
         border: none;
         color: white;
         padding: 10px;
         border-radius: 5px;
         cursor: pointer;
      }

      .clients-and-events {
         display: flex;
         gap: 20px;
         margin-bottom: 20px;
      }

      .table {
         width: 100%;
         border-collapse: collapse;
         margin-top: 10px;
      }

      .table th,
      .table td {
         border: 1px solid #ddd;
         text-align: center;
      }

      .table th {
         background-color: #f2f2f2;
      }

      .footer {
         text-align: right;
         margin-top: 20px;
      }

      .more-section {
         background-color: rgb(255, 255, 255);
         box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
         width: 405px;
         padding: 20px;
         color: rgb(0, 0, 0);
         border-radius: 8px;
      }

      .sections {
         border: 3px solid;
         border-image: linear-gradient(45deg, #34A853, #4285F4) 1;
         padding: 20px;
         border-radius: 8px;
      }

      .sections-header {
         display: flex;
         align-items: center;
         justify-content: space-between;
      }

      .sections-header h3,
      .sections-header span,
      .sections-header p {
         margin: 0;
      }

      .sections-header button {
         background: linear-gradient(45deg, #34A853, #4285F4);
         border: none;
         color: white;
         padding: 10px;
         border-radius: 5px;
         cursor: pointer;
      }

      .fourcards {
         background-color: red;
         display: flex;
         color: rgb(0, 0, 0);
         flex: 1;
      }

      .cardx {
         background-color: white;
         border-style: 1px solid;
      }

      .metrics .card {
         flex: 1 1 calc(25% - 20px);
      }

      .adbutton {
         background: linear-gradient(45deg, #34A853, #4285F4);
         border: none;
         color: white;
         padding-left: 10px;
         padding-right: 10px;
         padding-top: 5px;
         padding-bottom: 5px;
         border-radius: 5px;
      }

      .overview .card h3 {
         font-weight: bolder;
         font-size: 40px;
      }

      .bottom-menu {
         margin-top: auto;
      }

      .bottom-menu .nav-link img {
         width: 80px;
      }

      /* Seach Button space */
      .search-container input {
         margin-right: 10px;
      }

      .btn-success {
         background-color: #4CAF50;
         border-color: #4CAF50;
      }

      .btn-secondary {
         background-color: #f1f3f5;
         color: #333;
         border: none;
      }

      .table-responsive {
         overflow-x: auto;
      }

      .table {
         width: 100%;
         margin-bottom: 1rem;
         vertical-align: middle;
      }

      .table th,
      .table td {
         padding: 0.75rem;
         vertical-align: middle;
      }

      table thead th {
         vertical-align: bottom;
      }
   </style>
</head>

<body>
   <!-- Sidebar Section -->
   <div class="sidebar">
      <div class="logo">
         <img src="/images/bge-logo-tr.png" alt="BGE Logo">
      </div>
      <div class="profile">
         <img src="<%= user.profilePicture %>" alt="Profile Picture">
         <div>
            <h3>
               <%= user.name %>
            </h3>
            <span class="role">Admin</span>
         </div>
      </div>

      <nav class="nav flex-column">
         <a class="nav-link text-white" href="/dashboard"><i class="fas fa-tachometer-alt me-2"></i>Dashboard</a>
         <a class="nav-link text-white" href="/hr-department"><i class="fa-solid fa-book-open-reader"></i>
            Management</a>
         <a class="nav-link text-white" href="/members"><i class="fa-solid fa-address-card"></i>Members</a>
         <a class="nav-link text-white" href="/departments"><i class="fa-solid fa-house"></i>Departments</a>
         <a class="nav-link text-white" href="/services"><i class="fa-solid fa-briefcase"></i>Services</a>
         <a class="nav-link text-white" href="/projects"><i class="fa-solid fa-folder-open"></i>Projects</a>
         <a class="nav-link text-white" href="/clients"><i class="fa-solid fa-user"></i>Clients</a>
         <a class="nav-link text-white" href="#"><i class="fa-solid fa-square-poll-vertical"></i>Reports</a>
      </nav>
      <!-- IT Department Dropdown -->
      <div class="btn-group">
         <button class="btn btn-secondary btn-sm dropdown-toggle" type="button" id="itDepartmentDropdown"
            data-bs-toggle="dropdown" aria-expanded="false">
            IT Department
         </button>
         <ul class="dropdown-menu" aria-labelledby="itDepartmentDropdown">
            <li><a class="dropdown-item" href="/it/item-inventory">Inventory</a></li>
            <li><a class="dropdown-item" href="/it/item-distributed">Item Distributed</a></li>
            <li>
               <hr class="dropdown-divider">
            </li>
            <li><a class="dropdown-item" href="/it/category-management">Category Management</a></li>
         </ul>
      </div>
      <!-- GreatDay  -->
      <div class="bottom-menu">
         <a class="nav-link text-white" href="https://app.greatdayhr.com/" target="_blank">
            <img src="/images/GREAT-DAY-LOGO.png" alt="Time in - Time Out"><u>Attendance</u>
         </a>
      </div>
   </div>
   <div class="content">
      <div class="header">
         <h1>Category Management System</h1>
         <div>
            <span>Admin, <a href="/auth/logout">Logout</a></span>
         </div>
      </div>
      <!-- Announcement Section -->
      <div class="announcement-container">
         <% if (announcements && announcements.length> 0) { %>
            <% announcements.forEach(announcement=> { %>
               <div class="announcement-item">
                  <h3>
                     <%= announcement.title %>
                  </h3>
                  <div class="announcement-description"><%- announcement.description %></div>
               </div>
               <% }) %>
                  <% } else { %>
                     <p>No announcements available.</p>
                     <% } %>
      </div>

      <!-- New Category designa and functionality -->
      <div class="col d-flex gap-2 text-light">
         <!-- Left Side: Add/Edit Form -->
         <div class="col-6 bg-dark rounded-3 p-5">
            <h4 id="formTitle">Inventory Categories</h4>
            <form id="categoryForm" action="/it/category-management/add" method="post">
               <input type="hidden" name="id" id="categoryId">
               <div class="mb-3">
                  <label class="form-label">Name</label>
                  <input type="text" class="form-control" name="name" id="categoryName" placeholder="Desktop" required>
                  <div id="nameHelpBlock" class="form-text text-light">
                     The name is how it appears on your site.
                  </div>
               </div>
               <div class="mb-3">
                  <label class="form-label">Slug</label>
                  <input type="text" class="form-control" name="slug" id="categorySlug" placeholder="desktop">
                  <div id="nameHelpBlock" class="form-text text-light">
                     The "slug" is the URL-friendly version of the name.
                  </div>
               </div>
               <div class="mb-3">
                  <label class="form-label">Parent Item Category</label>
                  <div class="col">
                     <input type="hidden" name="parent" id="parentCategory">
                     <button type="button" id="parentCategoryBtn" class="btn btn-primary dropdown-toggle ms-auto"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        None
                     </button>
                     <ul class="dropdown-menu">
                        <li><a class="dropdown-item parent-select" href="#" data-parent-id="">None</a></li>
                        <% categories.forEach(category=> { %>
                           <li><a class="dropdown-item parent-select" href="#" data-parent-id="<%= category._id %>">
                                 <%= category.name %>
                              </a></li>
                           <% }) %>
                     </ul>
                     <div id="nameHelpBlock" class="form-text text-light">
                        Select none if you want to create a Parent Category
                     </div>
                  </div>
               </div>
               <div class="mb-3 pt-2">
                  <label class="form-label">Description</label>
                  <textarea class="form-control" name="description" id="categoryDescription" rows="3"></textarea>
               </div>
               <div class="d-flex justify-content-between">
                  <button type="button" id="cancelEditBtn" class="btn btn-secondary d-none">Cancel</button>
                  <button type="submit" id="categoryFormBtn" class="btn btn-success">Create Category</button>
               </div>
            </form>
         </div>


         <!-- Right Side: Category List -->
         <div class="col-6 bg-dark rounded-3 p-5">
            <table class="table border-0">
               <thead class="bg-dark">
                  <tr>
                     <th scope="col">Name</th>
                     <th scope="col">Description</th>
                     <th scope="col">Count</th>
                     <th scope="col">Actions</th>
                  </tr>
               </thead>
               <tbody id="categoryTableBody" class="text-light">
                  <% categories.forEach(category=> { %>
                     <tr>
                        <td>
                           <div class="d-flex justify-content-between">
                              <span>
                                 <%= category.name %>
                              </span>
                              <button class="btn btn-sm btn-outline-light toggle-children"
                                 data-category-id="<%= category._id %>">+</button>
                           </div>
                           <div class="child-categories" id="children-<%= category._id %>"
                              style="display: none; padding-left: 20px;">
                              <% category.children.forEach(child=> { %>
                                 <div class="form-check">
                                    <input class="form-check-input child-category-check" type="checkbox"
                                       value="<%= child._id %>" id="child-<%= child._id %>"
                                       data-child-id="<%= child._id %>" data-name="<%= child.name %>"
                                       data-description="<%= child.description %>" data-parent="<%= child.parent %>">
                                    <label class="form-check-label" for="child-<%= child._id %>">
                                       <%= child.name %>
                                    </label>
                                 </div>
                                 <% }) %>
                           </div>
                        </td>
                        <td>
                           <%= category.description %>
                        </td>
                        <td>
                           <%= category.childCount %>
                        </td>
                        <td>
                           <a href="#" class="edit-category" data-category-id="<%= category._id %>"
                              data-name="<%= category.name %>"
                              data-parent="<%= category.parent ? category.parent._id : '' %>"
                              data-description="<%= category.description %>">Edit</a>
                           <a href="#" class="delete-category" data-category-id="<%= category._id %>">Delete</a>
                        </td>
                     </tr>
                     <% }) %>
               </tbody>
            </table>

            <!-- Pagination -->
            <nav aria-label="Category pagination" class="mt-3">
               <ul class="pagination justify-content-end">
                  <% if (hasPrevPage) { %>
                     <li class="page-item">
                        <a class="page-link" href="?page=<%= currentPage - 1 %>" aria-label="Previous">
                           <span aria-hidden="true">&laquo;</span>
                        </a>
                     </li>
                     <% } %>
                        <% for (let i=1; i <=totalPages; i++) { %>
                           <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                              <a class="page-link" href="?page=<%= i %>">
                                 <%= i %>
                              </a>
                           </li>
                           <% } %>
                              <% if (hasNextPage) { %>
                                 <li class="page-item">
                                    <a class="page-link" href="?page=<%= currentPage + 1 %>" aria-label="Next">
                                       <span aria-hidden="true">&raquo;</span>
                                    </a>
                                 </li>
                                 <% } %>
               </ul>
            </nav>
         </div>
      </div>

      <!-- Delete Modal -->
      <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
         <div class="modal-dialog">
            <div class="modal-content bg-dark text-light">
               <div class="modal-header">
                  <h5 class="modal-title" id="deleteModalLabel">Delete Category</h5>
                  <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"
                     aria-label="Close"></button>
               </div>
               <div class="modal-body">
                  <p>Categories can be deleted. If a category has children, the children can be reassigned or deleted
                     along with the parent. Are you sure you want to delete this category?</p>
                  <div id="reassignmentOptions" class="d-none">
                     <p>This category has child categories. What would you like to do with the child categories?</p>
                     <select class="form-select" id="reassignToCategory">
                        <option value="">Delete all child categories</option>
                        <% categories.forEach(category=> { if (!category.parent) { %>
                           <option value="<%= category._id %>">
                              <%= category.name %>
                           </option>
                           <% } }) %>
                     </select>
                  </div>
               </div>
               <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                  <button type="button" id="confirmDeleteBtn" class="btn btn-danger">Delete</button>
               </div>
            </div>
         </div>
      </div>


      <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
         integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
         crossorigin="anonymous"></script>
      <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>


      <script>
         $(document).ready(function () {
            // Handle parent category selection
            $('.parent-select').on('click', function () {
               const selectedText = $(this).text();
               const parentId = $(this).data('parent-id');
               $('#parentCategoryBtn').text(selectedText);
               $('#parentCategory').val(parentId);
            });

            // Quick edit functionality
            $('.edit-category').on('click', function () {
               const categoryId = $(this).data('category-id');
               const categoryName = $(this).data('name');
               const categoryParent = $(this).data('parent');
               const categoryDescription = $(this).data('description');

               $('#categoryId').val(categoryId);
               $('#categoryName').val(categoryName);
               $('#categorySlug').val(categoryName.toLowerCase().replace(/\s+/g, '-'));
               $('#parentCategoryBtn').text(categoryParent ? categoryParent.name : 'None');
               $('#parentCategory').val(categoryParent || '');
               $('#categoryDescription').val(categoryDescription);

               $('#formTitle').text('Editing Inventory Categories');
               $('#categoryFormBtn').text('Update Category');
               $('#cancelEditBtn').removeClass('d-none');
            });

            // Edit child category functionality
            $('.child-category-check').on('change', function () {
               if ($(this).is(':checked')) {
                  const childId = $(this).data('child-id');
                  const childName = $(this).data('name');
                  const childParent = $(this).data('parent');
                  const childDescription = $(this).data('description');

                  $('#categoryId').val(childId);
                  $('#categoryName').val(childName);
                  $('#categorySlug').val(childName.toLowerCase().replace(/\s+/g, '-'));
                  $('#parentCategoryBtn').text(childParent ? childParent.name : 'None');
                  $('#parentCategory').val(childParent || '');
                  $('#categoryDescription').val(childDescription);

                  $('#formTitle').text('Editing Inventory Categories');
                  $('#categoryFormBtn').text('Update Category');
                  $('#cancelEditBtn').removeClass('d-none');
               }
            });

            // Cancel edit functionality
            $('#cancelEditBtn').on('click', function () {
               $('#categoryId').val('');
               $('#categoryName').val('');
               $('#categorySlug').val('');
               $('#parentCategoryBtn').text('None');
               $('#parentCategory').val('');
               $('#categoryDescription').val('');

               $('#formTitle').text('Inventory Categories');
               $('#categoryFormBtn').text('Create Category');
               $(this).addClass('d-none');
            });

            // Toggle child categories
            $('.toggle-children').on('click', function () {
               const categoryId = $(this).data('category-id');
               $(`#children-${categoryId}`).toggle();
               $(this).text($(this).text() === '+' ? '-' : '+');
            });

            // Delete category functionality with modal and reassignment options
            let deleteCategoryId = null;
            let isParentCategory = false;

            $('.delete-category').on('click', function () {
               deleteCategoryId = $(this).data('category-id');
               isParentCategory = $(this).data('parent') === '';
               $('#reassignmentOptions').toggleClass('d-none', !isParentCategory);
               $('#deleteModal').modal('show');
            });

            // Update delete category functionality
            $('#confirmDeleteBtn').on('click', function () {
               if (deleteCategoryId) {
                  const reassignTo = $('#reassignToCategory').val();

                  $.ajax({
                     url: `/it/category-management/delete/${deleteCategoryId}`,
                     type: 'POST',
                     data: { reassignTo },
                     success: function () {
                        location.reload();
                     },
                     error: function (jqXHR, textStatus, errorThrown) {
                        console.error('AJAX Error:', textStatus, errorThrown);
                        console.error('Response:', jqXHR.responseText);
                        alert('Error deleting category. Please check the console for more details.');
                     }
                  });
               }
            });
            // Show Edit and Delete options on hover
            $('tbody tr').hover(
               function () {
                  $(this).find('.edit-category, .delete-category').css('visibility', 'visible');
               },
               function () {
                  $(this).find('.edit-category, .delete-category').css('visibility', 'hidden');
               }
            );
         });
      </script>



</body>

</html>