<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-image: url("/images/About us Background.png");
            background-repeat: no-repeat;
            background-size: cover;
            font-family: Arial, sans-serif;
            margin: 0;
        }

        .forgot-form {
            width: 100%;
            max-width: 400px;
            padding: 40px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            align-items: center;
            display: flex;
            flex-direction: column;
        }

        .forgot-form h2 {
            margin: 0 0 20px 0;
            font-size: 24px;
            text-align: center;
        }

        .forgot-form p {
            text-align: center;
        }

        .forgot-form .form-control, .forgot-form .form-button, .forgot-form .google-login {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            box-sizing: border-box;
        }

        .forgot-form .form-control {
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .form-button {
            background: linear-gradient(to right, #43cea2, #185a9d);
            border: none;
            color: white;
            border-radius: 4px;
            cursor: pointer;
        }

        .forgot-form .google-login {
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid #ccc;
            border-radius: 4px;
            cursor: pointer;
        }

        .forgot-form .google-login img {
            width: 20px;
            height: 20px;
            margin-right: 10px;
        }

        .forgot-form a {
            color: #43cea2;
            text-decoration: none;
            display: block;
            text-align: center;
            margin: 10px 0;
        }

        .forgot-form a:hover {
            text-decoration: underline;
        }

        .icon {
            border-radius: 8px;
            padding: 5px;
            background: rgba(255, 255, 255, 0.2);
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(2px);
            -webkit-backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        header {
            position: absolute;
            top: 10px;
            left: 10px;
        }

        header img {
            width: 150px;
            padding: 8px;
            background: rgba(8, 7, 7, 0.65);
            border-radius: 8px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(5.9px);
            -webkit-backdrop-filter: blur(5.9px);
            border: 1px solid rgba(8, 7, 7, 0.3);
        }

        p {
            color: #b9b9b9;
        }
    </style>
</head>
<body>
    <header>
        <a href="/auth/login" method="get">
            <img src="/images/bge-logo-tr.png" alt="Logo">
        </a>
        
    </header>
    <div class="forgot-form">
        <h2>Forgot Password</h2>
        <p>Enter your email address to reset your password</p>
        <form action="/login.ejs" method="POST">
            <input type="email" name="email" placeholder="email.bgecorp@gmail.com" class="form-control" required>
            <button type="submit" class="form-button">Send Email</button>
            <p><input type="checkbox" id="checked">By clicking continue, you agree to our Terms of Service and Privacy Policy</p>
        </form>
    </div>
</body>
</html>
